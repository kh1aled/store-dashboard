<?php require_once __DIR__.'/../../config/database.php';require_once __DIR__.'/../../includes/functions.php';require_permission($pdo,'manage_products');verify_csrf();$id=(int)($_POST['id']??0);try{$st=$pdo->prepare("DELETE FROM products WHERE id=?");$st->execute([$id]);flash('success','Product deleted.');}catch(Throwable $e){flash('danger','Product cannot be deleted because it is used by an order.');}redirect('/admin/products/index.php');